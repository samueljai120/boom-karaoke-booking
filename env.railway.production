# Railway Production Environment Variables
NODE_ENV=production

# Railway automatically sets PORT, so we don't need to set it here
# PORT will be provided by Railway runtime

# API Configuration - Update these with your actual backend URLs
VITE_API_BASE_URL=https://your-backend-api.railway.app/api
VITE_WS_URL=https://your-backend-api.railway.app

# If you're using mock data (standalone mode), you can leave these as default
# VITE_API_BASE_URL=http://localhost:3001/api
# VITE_WS_URL=http://localhost:3001
